<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.burningcode.jira.issue.customfields.impl</groupId>
    <artifactId>jira-watcher-field</artifactId>
    <version>3.0.0-rc3</version>

    <name>JIRA Watcher Field</name>
    <packaging>atlassian-plugin</packaging>
    <description>Custom field type that allows watchers to be modified on the creation and update of issue.</description>
    <url>https://github.com/alanmosely/jira-watcher-field-plugin</url>

    <licenses>
        <license>
            <name>BSD-3-Clause</name>
            <url>https://github.com/alanmosely/jira-watcher-field-plugin/blob/master/LICENSE.TXT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Ray Barham</name>
            <email>rbarham@burningcode.com</email>
            <url>www.burningcode.com</url>
            <roles>
                <role>Original author (2008-2012)</role>
            </roles>
        </developer>
        <developer>
            <name>Alan Mosely</name>
            <url>https://github.com/alanmosely</url>
            <roles>
                <role>Maintainer (Jira 10/11 compatibility)</role>
            </roles>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/alanmosely/jira-watcher-field-plugin.git</connection>
        <developerConnection>scm:git:git@github.com:alanmosely/jira-watcher-field-plugin.git</developerConnection>
        <url>https://github.com/alanmosely/jira-watcher-field-plugin</url>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/alanmosely/jira-watcher-field-plugin/issues</url>
    </issueManagement>

    <!-- Some Jira transitives reference timestamped javax-era builds that only existed on the
         now-defunct java.net repositories. Pin them to equivalent versions that exist on Maven
         Central; they are provided-scope transitives and are never packaged into the plugin. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>javax.xml.bind</groupId>
                <artifactId>jaxb-api</artifactId>
                <version>2.3.1</version>
            </dependency>
            <dependency>
                <groupId>org.glassfish.jaxb</groupId>
                <artifactId>jaxb-runtime</artifactId>
                <version>2.3.1</version>
            </dependency>
            <dependency>
                <groupId>com.sun.istack</groupId>
                <artifactId>istack-commons-runtime</artifactId>
                <version>3.0.7</version>
            </dependency>
            <dependency>
                <groupId>com.sun.xml.fastinfoset</groupId>
                <artifactId>FastInfoset</artifactId>
                <version>1.2.15</version>
            </dependency>
            <!-- jira-api/jira-core reference forked builds (3.1-jenkins-3, 2.10.9.4.10)
                 that only exist on the Jenkins repo and Atlassian's internal repo. Pin the
                 equivalent Maven Central releases; both are provided-scope transitives the
                 plugin never touches or packages. -->
            <dependency>
                <groupId>commons-httpclient</groupId>
                <artifactId>commons-httpclient</artifactId>
                <version>3.1</version>
            </dependency>
            <dependency>
                <groupId>net.sf.ehcache</groupId>
                <artifactId>ehcache</artifactId>
                <version>2.10.9.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.atlassian.jira</groupId>
            <artifactId>jira-api</artifactId>
            <version>${jira.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>com.atlassian.jira</groupId>
            <artifactId>jira-core</artifactId>
            <version>${jira.version}</version>
            <scope>provided</scope>
            <exclusions>
                <!-- OpenSAML 5 is only published on the Shibboleth repository, not Maven
                     Central, and this plugin does not touch SSO/SAML code. -->
                <exclusion>
                    <groupId>org.opensaml</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
                <!-- The javax-era jndi jar (1.2.1) no longer exists on any public
                     repository; javax.naming has shipped with the JDK for decades. -->
                <exclusion>
                    <groupId>jndi</groupId>
                    <artifactId>jndi</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.atlassian.plugin</groupId>
            <artifactId>atlassian-spring-scanner-annotation</artifactId>
            <version>${atlassian.spring.scanner.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.inject</groupId>
            <artifactId>jakarta.inject-api</artifactId>
            <version>2.0.1</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>5.11.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- Every Atlassian artifact (jira-api/jira-core, AMPS, spring-scanner) lives on
         packages.atlassian.com, not Maven Central. Declaring the repositories here keeps
         the build working with plain Maven (and CI), not just the SDK's atlas-mvn. -->
    <repositories>
        <repository>
            <id>atlassian-public</id>
            <url>https://packages.atlassian.com/mvn/maven-external/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>
    <pluginRepositories>
        <pluginRepository>
            <id>atlassian-public</id>
            <url>https://packages.atlassian.com/mvn/maven-external/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>
    <build>
        <plugins>
            <plugin>
                <groupId>com.atlassian.maven.plugins</groupId>
                <artifactId>jira-maven-plugin</artifactId>
                <version>${amps.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <productVersion>${jira.version}</productVersion>
                    <productDataVersion>${jira.version}</productDataVersion>

                    <!-- Only affects atlas-run/atlas-debug dev loops; has no effect on the packaged jar -->
                    <enableQuickReload>true</enableQuickReload>

                    <instructions>
                        <Atlassian-Plugin-Key>${atlassian.plugin.key}</Atlassian-Plugin-Key>

                        <!-- Nothing is exported; this plugin has no public API packages -->
                        <Export-Package/>

                        <Import-Package>*</Import-Package>

                        <Spring-Context>*</Spring-Context>
                    </instructions>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.atlassian.plugin</groupId>
                <artifactId>atlassian-spring-scanner-maven-plugin</artifactId>
                <version>${atlassian.spring.scanner.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>atlassian-spring-scanner</goal>
                        </goals>
                        <phase>process-classes</phase>
                    </execution>
                </executions>
                <configuration>
                    <verbose>false</verbose>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <properties>
        <!-- Jira 11.3 is the current LTS line; it requires JDK 21, Spring 6 and Jakarta EE 10 -->
        <jira.version>11.3.10</jira.version>
        <amps.version>9.13.0</amps.version>
        <!-- Must match the spring-scanner runtime bundled with the target Jira version -->
        <atlassian.spring.scanner.version>6.0.2</atlassian.spring.scanner.version>
        <!-- This property ensures consistency between the key in atlassian-plugin.xml and the OSGi bundle's key. -->
        <atlassian.plugin.key>${project.groupId}.${project.artifactId}</atlassian.plugin.key>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
    </properties>
</project>